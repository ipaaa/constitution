# AGENTS.md

給所有在此專案工作的 AI agent 與協作者。先讀這份，再動手。

專案名稱：憲庭加好友。內容是台灣憲法法庭的公民教育網站。技術是 Next.js。
受眾是工作夥伴，其中包含法學背景者。網站尚未正式對外發布。

---

## ⛔ 絕對不要做的事

### 1. 不要自己執行內容同步

```
npm run sync-content            ← 會覆寫 src/data/*.json
node scripts/sync-content.mjs
```

**`npm run build` 已經安全**（2026-09-02 解除禁令）。PR #32 把同步移出了 build，
現在 `build` 就是 `next build`。已於 main 上實測：`npm run build` 執行前後，
`src/data/*.json` 的 sha256 完全相同。

同步仍然只在有人明確要發布內容時才執行，而且**跑完必須開 PR 讓 captain 對 diff**，
不可直接提交。流程見 `docs/content-pipeline/design.md` 第五節。

驗證改動可用 `npx tsc --noEmit`、`npm run dev`、`npm run build`。

### 2. 不要手改 `src/data/*.json`

那兩個檔是產物，不是原始資料。原始資料在 Google 試算表。
手改的內容在下次 sync 時會消失。過去已經發生兩次。

要改內容，改試算表。

### 3. 不要把設計文件裡的範例當成真實內容

2026-04-30 發生過一次：`015` 的設計文件有一節叫「Sample data shape」，
其中用「某學者」當佔位值說明資料結構。實作時那段被直接寫進 `discussions.json`，
並且上線顯示了四個月。

看到 `某學者`、`某大學法律系`、`test`、`lorem ipsum`、`快速了解最新判決的5個重點`，
一律視為佔位資料，不可發布。

### 4. 不要移除 `src/app/layout.tsx` 的 noindex

```ts
robots: { index: false, follow: false },
```

現在刻意保持。網站尚未對外發布，不要讓搜尋引擎收錄。
正式發布時才移除。追蹤項目見 `docs/health-check/TODO.md` 的 P3-8。

---

## 怎麼跟 captain 溝通

**不要預設 captain 懂技術。他不是工程師。**

### 用他聽得懂的話

- 先講**結果和影響**，再講技術原因。他要知道的是「會怎樣」，不是「怎麼做到的」
- 出現技術名詞時，當場用一句話解釋。不要留著讓他猜
- 不要丟程式碼要他判斷。要他看程式碼時，先說那段在做什麼
- 用生活化的比方說明機制，但**不要用比方取代事實**。比方之後要接具體證據

**壞例子**：「`build` script 會執行 sync，導致 `discussions.json` 被 CSV 覆寫。」
**好例子**：「只要有人更新網站，系統就會自動從試算表重抓資料，蓋掉現在的內容。這件事沒有人在把關。」

### 給選項，不要給一堆資訊

要他做決定時：

1. **先給一句話的建議**，講清楚你建議哪一個
2. 每個選項最多兩三行。寫**選了會怎樣**，不要寫技術細節
3. 明講**哪些是不可逆的**，哪些改壞了可以退回
4. 選項不要超過三個
5. 如果某個決定其實不重要，不要問他。自己決定，做完告訴他一聲

### 他需要知道的事，主動講

- 事情做壞了，馬上講。不要等他問
- 發現他先前的決定有問題，直接說，並附證據
- 你不確定的地方，說「我不確定」，並說明不確定什麼

---

## 語言與寫作規範

**與 captain 對話用繁體中文。文件也用繁體中文。**

寫作採簡明技術中文，即 ASD-STE100 的中文等效。規則如下：

| 規則 | 說明 |
|---|---|
| 一句一義 | 一個句子只講一件事 |
| 句子要短 | 目標 25 字以內 |
| 用主動語態 | 寫「sync 會覆蓋檔案」，不寫「檔案會被覆蓋」 |
| 用詞固定 | 同一個概念只用同一個詞。不要為了文采換詞 |
| 動詞開頭 | 指令句用動詞開頭。寫「執行 X」，不寫「你可以考慮執行 X」 |
| 不用比喻 | 不用修辭、不用譬喻、不用反問 |
| 不用模糊詞 | 避免「可能」「應該」「大致上」。真的不確定時，寫明不確定什麼 |
| 專有名詞保留原文 | 檔名、欄位名、指令、程式碼不翻譯 |

**引用編號項目時，必須指明是哪一份文件的編號。**
寫「`docs/health-check/TODO.md` 的 P0-2」，不寫「P0-2」。
專案內有多份文件各自編號，只寫編號會造成誤解。

---

## 文件規範

**`docs/INDEX.md` 是文件總索引。新增或刪除文件時，同步更新它。**

每一筆索引必須包含：

| 欄位 | 說明 |
|---|---|
| 路徑 | 檔案位置 |
| 用途 | 一句話 |
| 狀態 | `evergreen`（常青，需持續維護）或 `plan`（計畫，完成後封存）或 `record`（歷史記錄，不再更動） |
| 負責人 | 誰維護 |
| 最後查核 | 上次確認內容仍正確的日期 |

**狀態的意義：**

- `evergreen` — 描述現況。內容變了就要更新。過時的 evergreen 文件是危險的。
- `plan` — 描述打算做的事。做完就封存，不要留著讓人以為還沒做。
- `record` — 記錄某個時間點發生的事。**不要改寫**。要修正就追加補述。

**修正既有文件的原則：不要悄悄改寫原文。**
`docs/health-check/2026-08-31-content-pipeline.md` 有示範：判斷錯了就追加一節補述，
說明原判斷、新事實、以及為什麼改變。原文保留。
把錯誤的前提悄悄改掉，文件會看起來一直都對，反而失去參考價值。

---

## 實作方式

**要動程式碼時，用 spacedock 開發流程，不要直接開工。**

現有兩個 workflow，目前皆休眠：

- `docs/constitution-features/` — 網站功能。**已於 2026-09-02 refit 至 0.28，可使用**
- `docs/design-assets/` — 視覺素材。仍為 0.9.5，要用之前需先 refit

**內容產線的施工走 `constitution-features` workflow**（2026-09-02 captain 決定，
推翻 8/31「暫不進 workflow」的舊決定 —— 該決定的前提是 workflow 版本過舊，已於
2026-09-02 refit 解除）。施工規格見 `docs/content-pipeline/design.md` 第五節。

---

## 文件地圖

| 想知道什麼 | 看哪一份 |
|---|---|
| 內容產線出了什麼事 | `docs/health-check/2026-08-31-content-pipeline.md`。先看文件上方的兩則補述 |
| 還有什麼要做 | `docs/health-check/TODO.md` |
| 新產線要長怎樣 | `docs/content-pipeline/design.md` |
| 搶救出來的內容 | `docs/content-rescue/` |
| 專案定位、架構、技術、協作 | `docs/project/` |
| 全部文件的索引 | `docs/INDEX.md` ← **不確定去哪找就看這份** |

⚠️ `docs/_archive/` 內是已過時的文件，保留供追溯。**不要照做。**
每份檔頭已註明它被誰取代。

⚠️ 這兩份各有一部分過時，檔頭有警告標明範圍：
`docs/project/tech-stack.md`（資料流章節）、
`docs/content-pipeline/data-collection-guide.md`（T1／T2 章節）。

---

## 給 captain 的提醒事項

以下項目卡在人，不是卡在程式。列於此避免遺忘：

- `docs/health-check/TODO.md` 的 P0-2 — 釋字第 272 號的法律內容錯誤，需法學背景者確認
- 同檔 P1-6 — 確認網站版貓頭鷹短評是否為 AI 生成
