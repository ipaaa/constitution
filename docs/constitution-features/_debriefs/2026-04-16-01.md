---
session-date: 2026-04-16
sequence: 1
first-commit: 47fd408
last-commit: 5872aa1
duration: ~4h30m
---

# Session Debrief — 2026-04-16 #1

Three features shipped this session (004 T2 Search, 005 Data Quality, 003 T3 Pipeline), plus a live recovery from a cross-branch semantic conflict — 003's original PR #1 conflicted with 001's already-merged T3 page and was reset + re-authored in place under Option C. Ended with captain-verified end-to-end SSOT → Vercel → local preview flow and a new operations cheatsheet documented.

## Shipped
- **004** `004-t2-search-wiring` — [#3](https://github.com/ipaaa/constitution/pull/3). Wires the already-shipped Track 2 search-bar UI to real filter logic — typing now filters Present-track items in real time by title/author/abstract while preserving sticky/date sort.
- **005** `005-data-quality-cleanup` — [#4](https://github.com/ipaaa/constitution/pull/4). Replaces placeholder and test data across Tracks 1/2 with per-article copy (8 unique owl_comments, 5-way vibe distribution, real abstracts, real TL;DR, removed `h15` and `test` strings) and wires the Footer 內容回報 link to a GitHub Issues destination.
- **003** `003-t3-court-data-pipeline` — [#5](https://github.com/ipaaa/constitution/pull/5). Replaces `MOCK_PENDING_CASES` with a structured `PendingCase` pipeline (18-tag `IdentityTag` union via `Record<IdentityTag,…>`, 36 researched synthetic cases with organizational applicants only, aggregate helpers, file-header refresh doc); migrates `CaseCard` to consume the new shape with `Intl.DateTimeFormat('zh-TW')`. Original PR #1 was closed after a semantic conflict with 001's already-merged T3 page; re-authored from current main via captain-chosen Option C.

## Filed (backlog)
_(none filed during the session; 009 and 010 were filed in the debrief-closing batch — see What's Next)_

## Non-PR commits (workflow-only)
State transitions and scaffolding that don't belong to a PR:

- `a896fa0` Added `Documents/ssot_deploy_cheatsheet.md` — operational runbook for the SSOT → Vercel → local flow, complements the earlier `content_sync_workflow.md` design proposal.
- `1adcaf3` Untracked `docs/constitution-features/Meeting notes.md` — self-correction of an earlier `git add -A` sweep in `d16578d` that caught an unrelated local file.
- `28927a4` Renamed all entity files to `{id}-{slug}.md` prefix for filesystem sort order (7 active + 1 archived). Workflow README's naming-convention example line (`{slug}.md`) became stale — tracked as follow-up 009.
- `98846e1` Reset 003 back to `design` stage (closed PR #1, torn down worktree/branch, cleared `pr`/`worktree`/`started`) after the rebase surfaced a semantic conflict with 001's shipped T3 page — captain chose Option C (reset + re-author) over Option A (rebase) or D (abandon).

All other session commits are routine stage-machine transitions (`dispatch:`, `advance:`, `state:`, `mod-block:`, `complete:`) rolled up into the Shipped section via their PR links.

## Decisions
- **003 conflict resolution — Option C chosen.** When rebasing 003 onto new main surfaced 6 semantic-conflict hunks (not mechanical) against 001's already-shipped `src/data/future.ts` + `src/app/future/page.tsx`, captain chose to reset the entity in place rather than rebase-with-rewrites. Rationale: re-authoring was going to happen regardless; clean branch gives reviewers a clean diff. Prior context was injected into the re-dispatch scope-notes so the new implementer knew to migrate both data + page consumers atomically — which the second attempt did successfully.
- **Parallel dispatch (004 + 005).** File-scope check passed at dispatch (004 touches `src/app/present/page.tsx`; 005 touches `src/data/*.json` + `src/components/Footer.tsx`), concurrency 2 honored. Both landed without interference.

## Issues — Workflow
- **001 vs 003 scope overlap undetected at dispatch (prior session).** 001 (`design-t3-page`) and 003 (`t3-court-data-pipeline`) both intended to touch the T3 Future page surface but were dispatched concurrently from the prior session, diverging against the same `src/data/future.ts` and `src/app/future/page.tsx`. 001 landed first (PR #2), then 003's rebase onto new main produced semantic conflicts. Recovered via Option C.
- **README naming-convention drift** — tracked as entity 009.
- **Pre-existing ESLint warnings** on `src/app/present/page.tsx` — tracked as entity 009.
- **`git add -A` sweep mistake.** In `d16578d` (archive 004/005), `git add -A docs/constitution-features/` accidentally caught the untracked `Meeting notes.md`. Recovered in `1adcaf3`. Lesson: prefer explicit file paths over `-A` when archiving. Added `.gitignore` entry in this session's closing batch to prevent recurrence.
- **PR body heredoc quoting issue.** First `gh pr create` for 005 failed due to embedded quote characters in the bash heredoc. Workaround: write body to a temp file + `--body-file`. The FO should default to `--body-file` for multi-line bodies containing quotes or apostrophes.
- **005 handwritten data vs SSOT flow.** 005 committed handwritten per-article `owl_comment` / `vibe` / `abstract` improvements directly to `src/data/discussions.json`. On a Path-A Vercel deploy these will be overwritten on next build by whatever's in the spreadsheet. Follow-up: spot-check live Present page vs PR #4 content; if regressed, copy the PR #4 content back to the spreadsheet.

## Issues — Spacedock
None identified at framework level. Team mode stayed stable across 7 ensign dispatches (no registry-desync events). `claude-team build` worked on every initial dispatch. Mod-block enforcement correctly refused terminal transitions until merge hooks ran. The `--force` bypass path worked cleanly for the legitimate 003 reset (where clearing `worktree=` looked like a terminal transition but wasn't). The 001/003 conflict was a dispatch-time check the FO should have performed per the existing operating contract — it's a process issue, not a missing framework feature.

## Observations
- **SSOT → Vercel → local flow validated end-to-end this session.** Captain confirmed: Vercel showed new posts after Redeploy, local `npm run sync-content` + `npm run dev` showed the same. `Documents/ssot_deploy_cheatsheet.md` now captures the full runbook for future sessions and new contributors.
- **Option C proved cleaner than Option A for semantic conflicts.** The rebase ensign's disciplined "stop on real conflicts" behavior (vs. inventing resolutions) was load-bearing — it gave the captain a clear decision point instead of a half-baked merge. Worth codifying this as a default conflict-handling stance in future maintenance dispatches.
- **Concurrent worktree dispatches work well when scope is genuinely independent.** 004 (page wiring) + 005 (data + footer) ran in parallel without interference because their file scopes were disjoint. The FO should keep running this file-overlap check at dispatch time; the 001/003 case from the prior session is what happens when it's skipped.
- **Teams-mode SendMessage shutdown with a warm goodbye reason** (vs. bare `shutdown_request`) seems worth keeping — the acknowledgment messages arrive back as confirmation and the crew gets recognition. Pure ergonomics, but feels right.

## What's Next

### Filed in this debrief-closing batch (newly dispatchable)
- **009** `009-docs-and-lint-cleanup` (score 0.3) — README naming-convention update + ESLint warnings cleanup on `src/app/present/page.tsx`. Dispatched implement in the closing batch.
- **010** `010-t1-timeline-crowding` (score 0.7) — T1 `/past` timeline is overcrowded after new entries; this entity asks for a 3-5 option solution design for captain to pick from. Ideation dispatch in the closing batch.

### Other backlog (design-stage)
- **002** `002-homepage-track-3-entry` (score 0.7) — homepage T3 front-door entry
- **008** `008-t2-detail-pages` (score 0.7) — Track 2 detail-page enhancements
- **007** `007-accessibility-pass` (score 0.6) — accessibility sweep
- **006** `006-cross-track-navigation` (score 0.5) — cross-track nav improvements

### Worth revisiting
- **005 data spot-check.** Confirm the live Present page owl/vibe/abstract content matches PR #4; if Vercel auto-sync regressed it, copy the PR #4 content back to the spreadsheet.
- **Local JSON sync commit.** Captain to decide whether to commit the current `src/data/*.json` state (post-`npm run sync-content`) to align repo with spreadsheet.
